import styled from "styled-components";

const StHeader = styled.div`
  height: 80px;
  background-color: #282828;
  width: 100vw;
  color: white;
  position: fixed;
  top: 0;
  left: 0;
`;

const Header = () => {
  return <StHeader>asdf</StHeader>;
};

export default Header;
